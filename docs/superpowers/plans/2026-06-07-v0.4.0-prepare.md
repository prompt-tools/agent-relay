# v0.4.0 发布准备计划

> 日期：2026-06-07 · 档位：S · 来源：P0+P1 分发就绪度完成后版本 bump

## 目标

将 package.json 版本从 0.3.0 bump 到 0.4.0，填写 CHANGELOG，更新 WORKLOG/MEMORY。npm 发布后续单独处理。

## 变更的提交（v0.3.0..HEAD，15 个）

| 类别 | 提交 | 说明 |
|------|------|------|
| feat | `bf534b9`, `1fe0ffa` | P0: scoped name/files/platform guards/README; P1: CI/JSDoc/deps |
| audit | `1679ebb`, `0ba83cc`, `cef31c2`, `83e0267` | Batch 1/2/3 全仓审计 |
| fix | `2b045dd` | PROMPTS.md CLI 语法修正 |
| docs | 8 个 | WORKLOG/handoff/operations/audit report/alignment |

## Task 1: 版本 bump

**文件**：`package.json`

**改动**：
- `"version": "0.3.0"` → `"version": "0.4.0"`

**方式**：直接编辑（不用 `npm version`，避免自动 git tag）

## Task 2: CHANGELOG

**文件**：`CHANGELOG.md`

**改动**：将 `## [Unreleased]` 替换为：

```markdown
## [Unreleased]

## [0.4.0] - 2026-06-07

### Added

- Scoped npm package name `@prompt-tools/agent-relay`
- `files` field in package.json (bin/, src/, mcp/, scripts/)
- `author`, `repository`, `homepage`, `bugs` fields
- Platform guard for launchd (macOS-only, graceful skip on Linux)
- `command -v` replaced with cross-platform `which`/`where` in setup.mjs
- English README with installation docs and optional CLI agents table
- Dynamic MCP version from package.json (no hardcoded string)
- GitHub Actions CI workflow (Node 20/22 × macOS/Ubuntu)
- npm publish workflow (triggered on GitHub Release)
- JSDoc on 8 source files (74 @param tags)

### Changed

- README rewritten in English; Chinese README moved to docs/README-zh.md
- Config example: `config.example.yaml` replaced with `config.example.json`

### Removed

- Deprecated `relay pull`, `relay complete`, `relay fail` CLI stubs (use `relay send`/`relay receive`)
```

底部链接表新增：
```
[0.4.0]: https://github.com/prompt-tools/agent-relay/releases/tag/v0.4.0
```
并更新 `[Unreleased]` 链接指向 `v0.4.0...HEAD`。

## Task 3: WORKLOG

**文件**：`docs/WORKLOG.md`

**改动**：追加新条目：

```markdown
## 2026-06-07 — v0.4.0 发布准备

**完成**（S 档）：
- package.json version bump 0.3.0 → 0.4.0
- CHANGELOG.md 填写 [0.4.0] 条目（P0+P1 distribution readiness + audit batches 1/2/3 归纳）

**待办**：npm org 创建 + NPM_TOKEN 配置后方可 publish。
```

## Task 4: MEMORY

**文件**：`docs/MEMORY.md`

**改动**：verified facts 表追加：

```
| v0.4.0 准备就绪 | version bump + CHANGELOG done；npm publish 待 org/token |
```

## 验收标准

1. `package.json` version = `"0.4.0"`
2. CHANGELOG 有 `[0.4.0] - 2026-06-07` 条目，底部链接正确
3. `npm test` 62/62 pass
4. `git diff` 只涉及上述 4 个文件
