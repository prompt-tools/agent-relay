---
description: agent-relay 硬约束 — 协议、编排、审查、上下文
alwaysApply: true
---

# agent-relay

## 必读（按序）

`docs/PRINCIPLES.md` → **`docs/AGENT-CONTRACT.md`** → `docs/MEMORY.md` → `docs/WORKLOG.md`

## 协议与主路径

- 仅 **send** + **receive**（含 claim）；回传 = 再次 send（`type: result|failed|progress`）
- 默认 E2E：**cursor → hermes**（不用 codex）

## 编排（硬约束）

- **主 Agent**：拆 Task、汇报、落盘、commit — **不自己写大 diff**
- **Subagent**：实现 + **两次独立审查**（规格 PASS/FAIL → code-reviewer + BASE/HEAD SHA）
- Important/Critical → fix subagent → **re-review** → 整批终审
- **禁止** 一次 review 糊弄多 Task；**禁止** 无 fresh `npm test` 说完成

## 上下文满（summary / 跨 Phase / 反复扫 transcript）

1. 写 MEMORY + WORKLOG（只增不改贴全文）
2. 停读 transcript；以 MEMORY/WORKLOG/ROADMAP 为真源
3. 必要时 `handoff`

## 代码

- 零新 npm 依赖；provider → `src/providers/` + `relayd.mjs`
- 完成前：`npm test` 全绿 + WORKLOG 一行

## 禁止

Ruflo 联邦、inbox/pull/complete、四 IDE 全家桶、无测试/无审查的「做完了」
