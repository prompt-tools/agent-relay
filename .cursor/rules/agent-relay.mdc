---
description: agent-relay 项目原则、范围与开发流程
alwaysApply: true
---

# agent-relay

## 必读

- `docs/PRINCIPLES.md`, `docs/FOCUS.md`, `docs/MEMORY.md`

## 协议

仅 **send** + **receive**（含 claim）。回传 = 再次 send（`type: result|failed|progress`）。无 inbox。

## 主路径

**cursor → hermes** E2E。默认不用 codex（用户无额度）。

## 代码

- Provider: `src/providers/` + 注册 `relayd.mjs`
- 测试: `npm test` 必须通过再声称完成
- 多步任务: writing-plans → subagent-driven-development

## 上下文 >75%（或出现 summary / 跨 Phase）

1. 更新 `docs/MEMORY.md` + `docs/WORKLOG.md`（只写新增，引用路径不贴全文）
2. 有意义改动则 commit
3. 停止重读 transcript；以 MEMORY/WORKLOG/ROADMAP 为真源
4. 会话过长 → `handoff` 技能写交接 + 建议技能
5. 回复精简，大段代码用 citation 不重复粘贴

## 编排（主 Agent 只管主线）

- **主 Agent**：拆 Task、优先级、对用户汇报、MEMORY/WORKLOG/commit
- **Subagent**：实现（gsd-executor/generalPurpose）、审查（code-reviewer）、探索（explore）
- 主 Agent 不自己写大 diff、不吞完整 review 输出；只合并 subagent 摘要

## 禁止

Ruflo 联邦、四 IDE 全家桶 setup、恢复 pull/complete、无测试的「做完了」。
