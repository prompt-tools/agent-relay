---
description: agent-relay 硬约束 — 协议、编排、审查、上下文
alwaysApply: true
---

# agent-relay

## 必读（按序）

`docs/PRINCIPLES.md` → **`docs/AGENT-CONTRACT.md`**（约束↔技能绑定）→ `docs/MEMORY.md`

## 执行前 MUST Read 技能

| 场景 | 技能 |
|------|------|
| 多 Task | `writing-plans` → `subagent-driven-development` |
| 每 Task 审查 | `spec-reviewer-prompt.md` → `requesting-code-review` + `code-reviewer` |
| 说完成 | `verification-before-completion` |
| 上下文满 | `handoff` |

## 编排

主 Agent 只编排；实现/双审查派 subagent。Red Flags 见 superpowers `subagent-driven-development`。

## 代码

- 零新 npm 依赖；provider → `src/providers/` + `relayd.mjs`
- 完成前：`npm test` 全绿 + WORKLOG 一行

## 禁止

Ruflo 联邦、inbox/pull/complete、四 IDE 全家桶、无测试/无审查的「做完了」
