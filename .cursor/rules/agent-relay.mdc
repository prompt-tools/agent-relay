---
description: agent-relay — Superpowers + Cursor Subagent only
alwaysApply: true
---

# agent-relay

## 体系（仅此两套）

- **Superpowers**：Read SKILL.md 再动手（`using-superpowers` 起手）
- **Cursor Subagent**：实现 / 双审查 / 修 review / explore

不用 GSD、OMX 或其它技能库。

## 必读

`docs/AGENT-CONTRACT.md` → `docs/PRINCIPLES.md` → `docs/MEMORY.md`

## 新功能

Research-First §0.1：调研 → 审 → 设计 → 审 → 计划 → 审 → 实现 → 再审（见 AGENT-CONTRACT）

## 每 Task

`subagent-driven-development`：Implementer → spec（generalPurpose）→ quality（**code-reviewer** + BASE/HEAD SHA）→ `verification-before-completion`（fresh `npm test`）

## 主 Agent

只编排、落盘、汇报；不写大 diff。

## 产品

send/receive only；E2E **cursor → hermes**；零新 npm 依赖

## 禁止

跳过双审查、无测试说完成、inbox/Ruflo/四 IDE 全家桶
